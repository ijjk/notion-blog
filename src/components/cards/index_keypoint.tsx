export default ({ title, description }) => (
  <div className="column">
    <div className="keypoint_card">
      <h3 className="a-title">{title}</h3>
      <p>{description}</p>
    </div>
  </div>
)
